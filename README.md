
This might contain everything you need to run your app locally.

## Run Locally
You should be able to run the game locally by executing: 
Install dependencies:
 `npm install`
 Run the app:
`npm run dev`
**Prerequisites:**  Node.js
